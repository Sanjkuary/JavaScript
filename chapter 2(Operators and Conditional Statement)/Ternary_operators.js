// Ternary operators is a special operators which work on the 3 operands. between ? and : (condition ? true output : false output)

 let age_2 = 16;

 let result = age_2 >= 18 ? "adult" : "Not adult";

console.log(result);

// OR (to print in one line)

let age_3 = 25;
age_3 >= 18 ? console.log("adult") : console.log("not adult"); // not preferd this kind of writting in real time.